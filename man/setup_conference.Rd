% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{setup_conference}
\alias{setup_conference}
\title{Set up a new conference directory}
\usage{
setup_conference(
  path,
  conference_name,
  hashtags,
  min_date = as.Date(paste0(format(Sys.Date(), "\%Y"), "-01-01")),
  posts_per_hashtag = 250L,
  exclude_accounts = character(0),
  exclude_accounts_regex = NULL
)
}
\arguments{
\item{path}{Character. Directory path to create. Must not already exist.}

\item{conference_name}{Character. Display name (e.g. \code{"NICAR 2026"}).}

\item{hashtags}{Character vector. Hashtags to track
(e.g. \code{c("#NICAR26", "#NICAR2026")}).}

\item{min_date}{Date. Earliest date for posts. Default: January 1 of
the current year.}

\item{posts_per_hashtag}{Integer. Posts to fetch per hashtag. Default: 250.}

\item{exclude_accounts}{Character vector. Account handles to always
exclude. Default: \code{character(0)}.}

\item{exclude_accounts_regex}{Character or \code{NULL}. Regex pattern for
account exclusion. Default: \code{NULL}.}
}
\value{
The path, invisibly. Called for side effects.
}
\description{
Creates a directory structure with template files for a new conference,
ready for Shiny Server deployment and cron job updates.
}
\examples{
\dontrun{
setup_conference(
  path = "~/my_conferences/nicar2026",
  conference_name = "NICAR 2026",
  hashtags = c("#NICAR26", "#NICAR2026"),
  min_date = as.Date("2026-01-01")
)
}
}
