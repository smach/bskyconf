% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_posts.R
\name{update_conference_posts}
\alias{update_conference_posts}
\title{Update conference posts from Bluesky}
\usage{
update_conference_posts(
  hashtags,
  data_dir,
  posts_per_hashtag = 250L,
  min_date = NULL,
  exclude_accounts = character(0),
  exclude_accounts_regex = NULL,
  raw_file = "previous_retrieved_posts.Rds",
  display_file = "table_posts.Rds",
  auth = bs_conference_auth(),
  verbose = TRUE
)
}
\arguments{
\item{hashtags}{Character vector. Hashtags to search for (e.g.
\code{c("#PositConf2025", "#PositConf")}).}

\item{data_dir}{Character. Path to the directory where data files
are stored. Must exist.}

\item{posts_per_hashtag}{Integer. Number of posts to fetch per
hashtag. Default: 250.}

\item{min_date}{Date or \code{NULL}. Exclude posts before this date from
the display table. Default: \code{NULL} (no filtering).}

\item{exclude_accounts}{Character vector. Account handles to always
exclude. Default: \code{character(0)}.}

\item{exclude_accounts_regex}{Character or \code{NULL}. Regex pattern for
account handles to exclude. Default: \code{NULL}.}

\item{raw_file}{Character. Filename for the raw deduplicated posts
archive within \code{data_dir}. Default: \code{"previous_retrieved_posts.Rds"}.}

\item{display_file}{Character. Filename for the display-ready table
within \code{data_dir}. Default: \code{"table_posts.Rds"}.}

\item{auth}{Authentication object. Default: \code{\link[=bs_conference_auth]{bs_conference_auth()}}.}

\item{verbose}{Logical. Print progress messages? Default: \code{TRUE}.}
}
\value{
Invisibly returns the display-ready \link[data.table:data.table]{data.table::data.table}.
}
\description{
Fetches recent posts for specified hashtags, deduplicates them,
checks for excluded accounts (bots, protected), merges with previously
saved posts, and produces a display-ready data file for the Shiny app.
}
\details{
This function is designed to be called from a cron job script. It reads
and writes data files in \code{data_dir}.
}
\examples{
\dontrun{
update_conference_posts(
  hashtags = c("#NICAR26", "#NICAR2026"),
  data_dir = "path/to/data",
  min_date = as.Date("2026-01-01")
)
}
}
